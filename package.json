{
  "name": "chaufeer",
  "version": "1.0.0",
  "description": "Chaufeer backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "free-port": "node scripts/free-port.js",
    "dev": "node scripts/free-port.js && nodemon server.js",
    "test:api": "node scripts/test-apis.js",
    "seed:admins": "node scripts/seed-admins.js",
    "setup": "npm install && node -e \"const fs=require('fs'); if(!fs.existsSync('.env')){fs.copyFileSync('.env.example','.env'); console.log('Created .env from .env.example - update your values and save.');} else {console.log('.env already exists - make sure MONGODB_URI is filled in and saved.');}\"",
    "vercel-build": "echo Build complete"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "google-auth-library": "^9.15.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.3",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
